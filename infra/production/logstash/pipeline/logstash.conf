input {
  tcp {
    port => 5044
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "elastic"
    password => "IK2Ov4NWNr4Jvi"
    index => "cos-logs"
  }
  stdout { codec => rubydebug }
}
